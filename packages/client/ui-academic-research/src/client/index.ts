/**
 * Academic-research report viewer plugin, browser half. The phase-4 surfaces —
 * research progress, evidence list, Brief review, report navigation — are not
 * wired yet, so this apply registers nothing. The standalone `render.ts`
 * renderer stays an internal helper consumed by tests until that surface lands.
 */

/** Browser plugin body — no slot registrations until the research UI wires in. */
export function apply(): void {}
