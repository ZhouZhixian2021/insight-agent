# Academic 逐条证据保留

## 问题与范围

用户确认由 A 暂代全部模块开发，并要求一条证据失败时仍使用同篇已成功的证据。离线排查发现 Transformer 最新保存回答有三条候选，其中一条把解析原文中的公式规范化，不能逐字匹配；另外两条可以独立核验，但整篇抛错导致两条合格证据也没有返回。本次保留已有全文获取、解析、哈希与严格逐字校验，不增加模型调用或重试。

## 已完成

- 证据模块逐条隔离空引文、无效片段序号和未匹配/歧义引文；只为合格条目生成定位、记录和卡片引用。
- 返回 `rejectedDrafts`，记录原始零基草稿序号、片段序号、代码和不含原文的原因。失败条目之后的合格条目继续处理。
- 工作流区分完整抽取、部分抽取和全部拒绝。部分成功的合格证据继续进入分析；全部拒绝不计为纳入论文，且其他论文继续。
- 每篇有拒绝的论文计一次失败抽取操作，不按被拒条目数重复统计。覆盖和报告限制披露保留/拒绝数量。
- Remote 和 Web 同步返回/展示合格数量、被拒条目及原因，部分成功不显示为全部成功。接口固定 JSON 样例、双语说明及已有决策记录同步更新。

## 验证

- 定向测试覆盖证据、工作流、Controller 与 Web：首轮 205/206 通过；更新旧的整篇失败预期后，该文件 56/56 复查通过，合计 206 项均通过。
- 全仓库类型检查通过。Academic 无密钥真实组合快照刷新和只读回放均通过。
- 完整文档检查 `doc-sync` 33/33 通过，受影响代码风格检查、公共导出 JSDoc 与差异空白检查通过；初轮检查发现的类型收窄、分隔符/缩进及中文链接目标问题已修正。
- 使用本机既有真实会话的两篇模型回答离线回放：Transformer 保留 2 条、拒绝 1 条；BERT 保留 3 条、拒绝 0 条。五条合格记录的哈希、定位和卡片引用关系通过检查。回放不发起网络或模型请求，不写回真实会话。

## 边界与下一步

部分成功不代表语义审核通过，也不保证报告已经回答全部 Plan 问题。JSON 语法或结构错误、模型截断和模型调用异常仍按整次模型回答失败处理，未实现对这些错误的逐条恢复。原始模型请求/结果仍是持久回放依据；本次未新增拒绝投影的跨运行持久化。下一步可通过正式 Web 入口确认页面与报告的实际表现；新的付费模型运行另行确认。本次未提交或推送。

## 真实 Web 验收

用户随后授权重启 Web 并验收。重新构建学术研究客户端包，停止旧服务，使用项目已有启动脚本和运行目录重启。通过原有已批准研究计划的会话，在正式学术研究入口提交两行精确查询 `1706.03762` 与 `1810.04805`；未修改模型路由、输出上限或重试策略。

- 2026-09-20 北京时间 16:14，本轮正常完成。arXiv 提供目标论文；OpenAlex 的两条查询均以 HTTP 429 结束，页面因此显示来源检索和整轮研究处理部分成功。两条来源失败没有阻断全文和抽取。
- 实际纳入两篇、可用全文两篇。Transformer 生成并保留三条证据，BERT 生成并保留五条证据；本轮无被拒草稿，页面证据抽取状态为成功，论文处理失败为空。
- 两篇模型回答均在第一次尝试以 `stop` 结束，输出分别为 7,173 和 15,101 tokens，未发生截断重试。一次成功不能证明长期稳定；本轮没有触发部分抽取分支，该分支仍由前述真实旧回答离线回放和定向测试覆盖。
- 页面生成待审核草稿。实际点击报告证据链接可展开对应原文、来源 URL、论文版本与定位标识；Markdown 正文包含来源和证据附录。
- 报告只引用八条合格证据中的四条 findings，形成一条对比记录。现有分析器要求同一类别有两篇独立论文才形成对比；本轮 methods 仅 BERT 有，相关三条方法证据未展开为正文分析，Transformer 的 metrics 也未纳入当前对比。报告仍是基线汇总，尚未完成按 Plan 问题生成中文跨论文洞察的目标，不能作为完整报告验收通过。

服务保留运行供用户查看。本轮只新增验收记录，没有追加模型重跑，没有修改来源、分析或报告实现，未提交或推送。
