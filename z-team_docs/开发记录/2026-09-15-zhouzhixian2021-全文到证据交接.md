# 全文到证据交接

成员 A 在 dev/zhouzhixian2021 上实现[单篇交接入口](../../packages/academic/workflow/src/index.ts)，基线为 88cde0f。本次不提交、不推送。

## 实现

- 新增 academic-workflow 库，extractPaperEvidence 接收归并版本、解析结果、明确的历史绑定状态和调用方生成器。
- 首次 not_extracted 且无历史内容绑定时补齐版本副本，保留 ID；哈希相同复用版本。
- 身份不符、空哈希、哈希冲突及需核对的历史状态返回 paused，不调用生成器。返回记录含新旧哈希、论文与版本 ID、来源和获取时间；尚未落盘。
- 正常路径直接调用 B 的 extractEvidenceFromContent，返回一致的版本与证据。取消和抽取失败向调用方传播，不自动创建新版本。
- 未修改 B、C 的业务代码或共享模型；新增包登记、双语文档、生成目录与决策记录。

## 验证

- 聚焦 Vitest：10 项通过；新增源码语句 18/18、分支 14/14、函数 2/2、行 13/13，均为 100%。测试实际连接 HTML 解析与证据抽取，并证明冲突论文之后其他论文仍可由调用方继续处理。
- 包源码与测试 TypeScript 检查、聚焦 oxlint、工作区 constraints 通过。
- 使用仓库 tsdown 配置按包过滤构建通过；plain Node 导入成功；publint 无诊断。
- test:docs：15/15 通过。doc-sync：32/33，通过项包含公开 JSDoc、双语、目录、链接与文档站点检查。
- 全仓构建及 doc-typecheck 被未修改的 client-ui-academic-research 包阻断：构建器找不到 lib/types/{index,invariant,startup}.js。不能据此宣称全仓构建通过。
- verify-package-invariants 中新包已无诊断；现有 analysis、eval、report、client-ui-academic-research 四个 README 缺少检查要求的无 companion 原因句，留待对应范围修复。

## 后续

本次只完成可调用、已测试的单篇交接，不等于完整研究流程或恢复系统。批次调用方仍需保存暂停记录、处理其他失败、接入模型请求日志和批准检查。哈希冲突后的人工核对与恢复继续在[待改进事项](../模块分工/member-a-academic-workflow-call-plan.md#待改进事项)跟踪。
