# Academic 按研究问题组织的洞察分析

日期：2026-09-20。负责人：成员 A（暂代 B、C 相关接入）。

## 完成内容

正式调用入口在逐篇证据核验后增加洞察模型阶段，保留既有来源检索、全文下载、解析、内容哈希和逐条证据保留机制。模型仍由 DSH 当前会话选择，不绑定具体供应商；洞察沿用当前模型输出预算和仅对输出截断重试的策略。

分析库新增准入、提示、严格 JSON 校验和 Claim 转换：逐个处理批准 Brief 的研究问题；单篇论文解释保留为来源陈述；跨论文综合至少由两篇独立论文支持。模型只引用本次准入证据。未知证据、错误 Brief 版本、缺失必需章节、越界序号和无支持陈述均被拒绝。缺少内容的章节必须明确说明原因，不用模型记忆补造事实。

报告按问题展示分析段落、引用、适用范围与不确定性，并列出问题覆盖和章节缺口。单篇来源陈述也接受版本、定位和哈希核验并计入覆盖，不能仅因没有跨论文 Claim 就误判不足；语义审核仍待完成。书目与证据附录由实际输入生成；正文字数不含标题、引用、附录或重复段落。不满足 Plan 的篇幅或问题覆盖会进入质量问题。模型标记 answered 不等于独立审核，当前仍为草稿。

会话增加 academic/synthesis-request 和 academic/synthesis-result，记录准确输入、原始响应、预算、用量、请求关联及结果。抽取与洞察复用同一套持久化传输逻辑。Remote 和网页增加独立 synthesis 状态及原因；不足或失败时保留论文成果并返回 report:null，不回退成模板报告。

## 验证

- 分析、报告、工作流、Remote 与网页定向回归：301/301 通过。
- 无密钥 academic-evidence 会话快照刷新及随后的 replay 均通过（各 1/1）；通过正式 sdk-minimal 配置验证抽取、洞察调用与报告渲染。新增洞察结果沿用已知模型流的采样时间规范化规则，引用、正文、用量与请求序号保持原样；相关规范化测试 66/66 通过。
- Python SDK 真实 DSH 进程投影：1/1 通过。进程退出有既有未关闭管道的 ResourceWarning，不影响本次断言。
- 完整 typecheck 通过；定向 oxlint 与公共导出 JSDoc 检查通过；文档门禁 31 项首次通过，另两项格式与双语代码块问题修正后复验通过；git diff --check 通过。

## 验收边界与下一步

实现阶段只进行了合成夹具验证；随后按用户要求完成了下面记录的真实 Web 验收。合成夹具不能证明真实学术结论。若证据无法支撑某题，应检查明确缺口，而非为了完整度放宽来源核验。独立语义审核、自动恢复与长论文分段仍未实现。

相关实现与固定字段见[交接说明](../模块分工/academic-synthesis-handoff.md)及[决策记录](../../.agents/notes/implemented/architecture/2026-09-20-academic-question-synthesis.zh.md)。

## 真实 Web 验收：未通过

2026-09-20 晚间重启本地 Web，复用会话 `session-eb3bd987-1cb3-4149-b8fd-009d127df013` 中已批准的 Transformer/BERT 研究计划，网页输入两条查询 `1706.03762`、`1810.04805`。模型保持当前会话的 `wowei/deepseek-v4-pro`，每次输出上限 16384 tokens，最多两次尝试；没有提高额度或修改模型参数。

- 检索：调用 arxiv 与 openalex，发现 20 条、去重后 8 篇，按候选上限保留 5 篇，实际纳入 2 篇，来源失败 0 次。截断标记仍为 true，不能将数量上限内的成功解释为穷尽检索。
- 全文与证据：Transformer 使用 HTML，BERT 使用 PDF；两篇全文成功解析，分别接受 3 条证据，无逐条拒绝。六条实际证据连同批准的两个研究问题进入洞察模型请求。BERT 证据仅覆盖 MLM 的高层描述、参数初始化与微调、十一项任务的概括结果，缺少更细的机制和实验条件。
- 第一次洞察：会话请求/结果序号 105/106，输出达到 16384 tokens，`finish=max-tokens`，最终 JSON 只有 3361 字符且不完整，记录 `EVIDENCE_MODEL_INCOMPLETE`。现有策略自动重试一次。
- 第二次洞察：序号 107/108，输出 13029 tokens，`finish=stop`；最终 JSON 完整，但记录 `SYNTHESIS_INVALID_MODEL_OUTPUT`。使用正式解析器对已记录输出离线复验，准确原因为 `Supporting independent works are insufficient for this statement kind.`：第 8、9 个分析段落将本轮局限/证据缺口标记为 synthesis，却只使用 background 引用，没有 supports。其余七段没有这项错误；当前整份校验失败会阻止整个报告，不会保留有效洞察段落生成草稿。
- 即使修复上述分类问题，候选分析正文按现有正文计数规则仅 1501 字（含适用范围与不确定性，不含引用、标题、附录与重复段落），低于 Plan 的 2000 字下限；BERT 问题被模型标为 partial。因此不能把这次候选输出宣称为符合计划的报告。
- 网页最终显示检索、全文、证据抽取成功，论文证据数各为 3，但未返回报告。本轮开始时学术面板仍使用旧构建，没有显示新增的洞察状态；之后完成 Web shell 构建和 academic-research 客户端 bundle，构建均通过，热更新重挂载后本轮页面结果不再保留。更新后的面板尚未通过另一轮真实调用验收，没有为验证显示而重复付费调用。

下一步应先让本轮限制和缺口使用恰当的输出位置，增加局部洞察失败时保留有效段落的策略，并让抽取证据更贴合研究问题；再验证正文充分性及模型输出预算。不能将 background 强行改成 supports，也不能靠重复段落满足篇幅。本轮仅运行验收、更新本地构建和记录问题，没有修改业务源码、提交或推送。

原始请求和响应保存在上述会话日志；本机诊断副本为 `.dsh-runtime/synthesis-live-session.json`、`.dsh-runtime/synthesis-live-attempt1.txt`、`.dsh-runtime/synthesis-live-attempt2.json`。这些是本机忽略目录中的验收材料，不作为仓库固定样例提交。

## 洞察局部恢复

按用户后续确认，补齐洞察段落的局部恢复。完整 JSON 与批准 Brief、问题顺序、章节和引用序号先通过整体验证，再逐段检查字段及证据支持。个别段落不合格时，记录原始候选序号、错误代码与原因，保留其他段落并重新映射引用；受影响的问题降为 partial 或 unanswered，空缺章节明确说明原因。不将 background 改成 supports，也不把无支持的局限或研究缺口作为 Claim。提示明确要求本轮缺口使用 limitations、问题 reason 或章节 missingReason。

会话结果增加 partially_validated 与主机生成的 rejectedStatements；旧事件以及 JSON 解析前的失败允许不含该字段。所有段落都被拒时，记录 failed 和 SYNTHESIS_NO_VALID_STATEMENTS，工作流保留论文与证据但不生成报告。仍有合格段落时，Remote 返回 synthesis.status=partial_success、拒绝原因与草稿。报告将被拒候选序号和原因加入局限及 unmet_plan，排除被拒正文；结构通过仍不代表满足 Plan 或语义审核通过。

对上次真实响应（结果序号 108）离线重放，保留 7 段，拒绝原第 8、9 段，得到 2 条跨论文 Claim 和 6 条引用证据。草稿保存在 `.dsh-runtime/transformer-bert-recovered-draft.md`，复验摘要为 `.dsh-runtime/synthesis-recovery-replay-summary.json`。正文按当前规则为 1176 字，仍低于 2000 字下限，BERT 问题仍为 partial，质量状态 needs_review；这份输出是保留成功成果的草稿，不是完整研究报告验收通过。本轮未新增付费模型调用，没有提交或推送。

分析、报告、工作流、Remote 和网页定向回归 303/303 通过；完整 typecheck、定向 oxlint、公共导出 JSDoc 检查通过。无密钥 academic-evidence 场景刷新与随后重放各 1/1 通过，场景包含合格段落和仅背景引用的被拒段落，并断言持久化诊断及草稿保留。模型输出预算、自动补充检索、证据抽取提示与独立语义审核不在本步修改范围。

后续收尾检查：Python SDK 真实 DSH 进程投影 1/1 通过，保留此前已有的未关闭管道 ResourceWarning；文档快速检查 15/15 通过，双语配对与生成目录已更新，git diff --check 通过。浏览器尚未再次调用修复后的完整真实流程；当前报告是已记录响应的离线恢复产物，下一轮新模型输出仍需真实验收。

## RAG 计划的版本筛选与审核前检查

真实会话 `session-c4320efd-4ddb-4bee-9a34-f7ffeac9fbf8` 在检索前失败。已批准的 Brief 使用模板提供的 conference_paper、journal_article，而筛选和洞察准入按 WorkVersion.versionType 比较，造成计划与执行规则不一致。语言 zh-CN、引用 numeric、篇幅 characters 和章节别名均可执行。

用户确认统一按预印本／录用稿／正式发表版本筛选。本次修正随附模板为 preprint、accepted_manuscript、version_of_record，明确不据 venue 猜测会议或期刊类别。allowPreprints=false 与撤稿排除规则继续生效；流水线在下载前也会拒绝自定义适配器选出的范围外版本。

共用要求检查支持拟议 Brief，不授予批准。Web 控制器挂载可释放的工具分发校验，携带 Academic Brief 围栏的 exit_plan_mode 在审核工具执行前完成解析与可执行要求检查；普通计划不受影响。读取已批准计划时再次检查，按字段返回中文错误及支持值，不静默改写历史计划。既有 RAG 计划需修订并由用户重新审核。

离线读取该会话批准计划，原值被明确拒绝；只将 includedWorkTypes 改为上述三种状态的副本通过检查，解析出 8 个必需报告章节。没有改写会话，也没有新增真实论文检索或模型调用。通过兼容性检查不代表达到该计划的至少 6 篇论文、3 篇全文和 6000 字报告要求。

验证：定向回归 125/125、完整 typecheck、定向 oxlint、公共导出 JSDoc、workspace constraints、文档快速检查 15/15 通过。新增 academic-plan-validation 无密钥快照通过真实 dsh sdk-minimal 配置验证旧类别值被拦截、正确版本值进入真实审核工具；最小 SDK 没有用户问答通道，后者明确返回缺少审核通道，未伪造批准。快照刷新与随后重放各 1/1 通过。未提交或推送。

已通过既有启动脚本重启 Web，地址保持 `http://127.0.0.1:60103/`，刷新后确认原 RAG 会话和已选模型正常显示。下一步由用户在当前会话重新进入 Plan，按最新模板提交采用三种版本状态的修订计划；用户批准后再从学术研究入口运行。未自动提交计划、授予批准或调用真实研究模型。

## 2026-09-21：证据不足时继续处理候选论文

本次 RAG 运行检索得到 30 条候选，首批 10 篇全文中 7 篇被范围检查排除、1 篇模型回答截断、2 篇提供有效证据。已批准计划要求至少 6 篇可用论文、其中至少 3 篇全文，因此洞察模型没有被调用，也没有报告。工作流在选择阶段按最终纳入上限截取候选，导致剩余候选未被尝试。

按用户要求，选择器保留批准候选上限内的有序队列，工作流逐篇获取全文、抽取并检查证据。被排除、失败、暂停、空证据以及未关联有效证据卡的记录不占成功纳入名额；已经成功的证据保留，未处理候选继续补入。同一套洞察准入逻辑提供可用论文 ID，同时用于停止判断和 Web 实际纳入统计，避免抽取状态与可用证据数量不一致。

计划启用证据达标即停止时，满足最低可用论文数与全文数后进入洞察分析；否则继续到成功纳入上限。候选用尽、批准的时间预算耗尽或用户取消也会结束处理，结果保留已处理论文及具体停止原因。本次仅在已有检索候选中补选，不自动扩大查询或重新检索。候选全部处理后仍低于计划最低要求时，继续保留证据不足状态；生成证据不足草稿属于后续独立工作。

验证：工作流、分析及 Remote 定向测试 262/262 通过，覆盖失败补位、空证据、未关联卡片、候选耗尽、候选上限、成功纳入上限、提前停止和时间取消。完整 typecheck、定向 oxlint、公共导出 JSDoc、workspace constraints、文档快速检查 15/15 通过，双语 README 与所属架构记录已同步配对。新增 academic-replenishment 无密钥启动快照，实际运行 DSH 最小 SDK 配置：前两篇分别被排除和抽取失败，第三、第四篇提供有效证据后生成草稿，第五篇未被调用；刷新及重放通过，最终构建后再次重放 1/1 通过。

通过既有启动脚本重启 Web，保持 `http://127.0.0.1:60103/`，网页刷新后正常加载。没有自动重新运行真实 RAG 研究或新增真实模型调用，没有提交或推送。下一次真实运行需要验证候选补选能否达到当前计划的证据要求。

## 2026-09-21：提交前收尾检查

提交前重新构建通过。补齐 Plan 审核校验监听器的事件关系矩阵、配置目录源代码行号，以及 Academic 包未发布 invariant 伴随模块的说明，并同步中文对侧和配对记录。包 invariants 检查通过。hygiene 中其余已通过项包括 publint、依赖、workspace constraints、构建后 invariants、运行时依赖闭包和客户端检查。NodeNext 检查通过忽略目录中的诊断副本确认失败于 Windows 创建临时目录符号链接，底层错误为 EPERM，尚未执行消费者类型检查；Cordis 配置检查失败于 Git 模式 120000 的 ACP cordis.yml 在本机被检出为链接目标文本。未修改这些平台文件或跳过检查。

真实 RAG 补选验收已经执行：15 篇全文中 3 篇提供 18 条证据、11 篇被范围检查排除、1 篇遭遇模型服务请求突增保护。候选补选生效，但当前洞察准入仍因少于 Plan 的 6 篇最低要求停止。continue_with_warning 的有限草稿、版本状态误排及模型限流分类与重试尚未实施，不属于本次提交完成声明。

## 2026-09-21：证据不足时按计划生成有限草稿

落实 continue_with_warning：证据准入返回 ready、ready_with_warning 或 blocked。仅有有效证据且不足时策略允许继续时，进入有限草稿分析；零有效证据及 stop_for_review 下的不足仍停止。补选仅在真正达到两项数量要求时提前停止，带警告准入不会提前结束候选处理。取消和超时仍保留已结束工作，不启动额外洞察调用。没有修改 Plan 下限、版本筛选、限流策略或查询编排。

模型提示由既有输入推导并记录具体数量缺口，要求逐题回答、说明部分回答与未回答原因，不为篇幅补造内容。报告由程序显示证据有限提示、纳入分析与正文引用及支持论文数量，数量缺口进入 unmet_plan。正文、原文、版本、哈希和引用验证保持原有规则；单篇来源陈述不冒充跨论文结论，全部段落不合格时仍无报告。正式交付继续受评测阻止，Web 对证据覆盖不足的草稿明确说明仍可查看和下载。未新增 Remote 返回字段或持久化事件字段。

验证：分析、工作流、报告和 Web 测试 279/279，Remote 测试 49/49；完整 typecheck、定向 oxlint、网页构建通过。doc-sync 初次为 32/33，唯一失败是快照辅助代码的 TypeScript 收窄比较，修正后完整 typecheck 与 doc-typecheck 单独复验通过。新增独立 academic-limited-draft 无密钥启动场景，使用正式 DSH SDK 最小配置验证不足时仍调用洞察模型、保留合格段落、生成有限草稿且最终交付被阻止；新旧场景只读重放 2/2 通过，原 academic-evidence 历史快照保持不变。

离线重新执行原 RAG 会话结果序号 121、125、129 的证据组装和原文定位校验，三篇各保留 6 条证据，共 18 条通过，无新增网络或模型请求。诊断摘要保存在忽略目录 .dsh-runtime/rag-evidence-revalidation.json。旧运行未持久化完整 AcademicWork 书目、WorkVersion 元数据、原 RetrievalRun 身份和完整覆盖观察，无法从当前日志原样恢复整份分析输入；没有编造缺失字段或声称已生成真实 RAG 报告。现有日志恢复能力限制后续单独处理。

Web 已在原端口 60103 重新启动，下一次研究执行新规则。真实模型质量仍待下一次运行验收。本轮未提交或推送。

## 2026-09-21：有限草稿真实 Web 验收

用户授权后，在原 RAG 会话 session-c4320efd-4ddb-4bee-9a34-f7ffeac9fbf8 中重新运行 retrieval augmented generation hallucination，沿用已批准 Plan 和已配置的 deepseek-v4-pro。没有修改至少 6 篇论文、至少 3 篇全文、6000—16000 字及 continue_with_warning 的计划要求。

本轮 arXiv 与 OpenAlex 检索成功，发现 60 条记录，候选上限保留 30 篇；15 篇进入全文获取和模型范围检查，11 篇被排除。4 篇模型各提出 6 条候选证据，原文定位检查拒绝其中 4 条，最终保留 6、3、5、6 条，共 20 条有效证据。网页将两篇局部定位失败记为失败操作，同时保留有效证据，没有将整篇成功成果丢弃。中间进度所述 24 条是模型提出数，最终有效数以原文校验后的 20 条为准。

证据请求及结果记录为会话序号 133—164；其中请求 155 的第一次响应达到 16384 tokens 上限，结果 156 为 EVIDENCE_MODEL_INCOMPLETE，按已有规则重试后结果 158 成功。未临时增加额度或重试次数。候选用尽且只有 4 篇有效论文时，工作流继续发起洞察请求 165，结果 166 为 validated，保留 23 个分析段落，其中 3 个为跨论文综合。证据不足不再阻止本轮生成草稿。

网页实际返回“已完成／部分成功／阻止交付”，生成《检索增强生成（RAG）能否减少大语言模型幻觉：方法、实际效果与未解决问题》草稿。Markdown 顶部明确写明证据有限、4 篇低于 Plan 至少 6 篇；纳入、引用和支持正文均为 4 篇，均提供全文证据。4 个研究问题均为部分回答，逐项列出直接幻觉率比较不足、命名方法覆盖不足及开放问题证据面有限等缺口。正文计数 2545 字，低于 6000 字下限；引用结构通过但独立语义审核仍未完成，因此不作为正式研究交付。

已在网页展开正文并验证下载文件实际落盘；本机验收副本为忽略目录 .dsh-runtime/rag-limited-draft-2026-09-21.md。结果页保持打开。本轮没有新增业务源码修改，没有提交或推送。后续应优先改善查询结果相关性、版本元数据与模型范围检查的衔接，以及抽取中引文定位和输出截断问题；不以降低 Plan 要求或拼接无证据内容满足数量与篇幅。
