# 开发记录：多行查询与覆盖文案

## 范围

成员 C，分支 dev/ykxy11。开发前获取 origin 并快进到 master 的 7b82b29085。实现并验证后，用户授权提交并推送到 dev/ykxy11；PR 由用户自行创建。

## 完成内容

- 研究查询控件由单行 input 改为 textarea，默认显示四行，可纵向调整高度。
- 新增中英文提示：“一行一条查询，最多三条，并受已批准研究计划限制。”通过 aria-describedby 与输入框关联。
- 保留现有 query 字段和请求方式。仅去除整个输入的首尾空白，内部换行保持不变，不把多行拼成一行，不新增 Remote 字段。
- 查询拆分、去重和已批准计划限额继续使用 A 的 Controller 校验，页面不自行裁掉第四条查询。
- 将“结果已被数量上限截断”改为“检索覆盖受限或提前截断”，同步调整英文和未标记限制时的文案。
- 新增两行查询的组件测试：验证 textarea、提示关联、Remote 收到的 query 包含换行，以及正常显示返回结果。
- 更新实际 Web 浏览器测试，以两行 Transformer/BERT 代表论文标题作为输入，检查多行控件、换行保留和真实 Controller 的前置条件错误回显。
- 更新中英文 README、对应 Agent Note 和翻译记录。

## 边界

没有修改 AcademicSourceSearchBatchResult、来源 Provider、Academic Model、工作流或 Controller，没有新增字段或请求。B 的 fetch failed、2017—2020 来源覆盖和匹配规则排查不在本次改动中。

两条测试查询用于验证页面输入传输，不代表已完成真实论文检索验收。仍需 A、B 的来源与已批准计划完成业务联调；没有消耗外部模型额度。

## 验证

- 模块测试：5 个文件、24 个测试通过。
- 宿主/客户端 TypeScript 检查通过。
- 完整构建通过；5 个改动代码/测试文件的定向 oxlint 通过，无错误或警告。
- 完整 doc-sync：33/33 通过；git diff --check 通过。
- 实际 Edge 浏览器测试 1/1 通过，截图已查看：E:/agent/academic-preview/academic-multiline.png。验证多行显示、提示、换行保留及真实 Controller 的未审核计划错误回显；没有把该错误场景描述成真实论文检索成功。
- 全量 GUI：294 个文件通过、1 个文件失败，4039 个测试通过、1 个跳过、1 个失败。失败为未改动的 code-block.client.spec.tsx 语法高亮加载测试超过 5 秒；没有改超时或重试规则，单独复验该文件 15/15 通过。没有宣称全量重跑通过。

## 查看

主 Web 选择已有会话，点击“学术研究”，在多行输入框中一行输入一条查询。现有模型选择和 Research Brief 审核前置条件保持不变。
