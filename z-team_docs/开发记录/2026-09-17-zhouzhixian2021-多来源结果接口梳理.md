# 开发记录：多来源检索结果交接

## 基本信息

| 项目 | 内容 |
|---|---|
| 日期 | 2026-09-17 |
| 负责人 | 成员 A，`ZhouZhixian2021` |
| 协作人员 | 成员 B（来源与证据）、成员 C（分析与产品） |
| 个人分支 | `dev/zhouzhixian2021` |
| 基线 | 远程 `master` 提交 `2a938ba` |

## 目标

在不修改 B 的 Provider 实现和 C 的页面实现的前提下，确定多来源部分成功、覆盖统计及 Web 返回值的最小交接规则，使 B、C 能在 A 的共享规则合并后并行开发。

## 完成内容

- 核对 A5 的 `BatchResult<T>`、`ProviderFailure`、`CoverageSummary` 和 `RetrievalRun`，确认现有共享类型足以表达本阶段结果，不新增重复类型或逐来源统计字段。
- 确定 B 的搜索批次需要额外提供实际调用的 Provider、上限前发现记录数、批处理结果、截断状态和来源声明的覆盖限制。
- 确定单个 Provider 失败时保留其他来源结果；全部 Provider 运行失败时返回失败批次；配置错误和用户取消不转换成普通来源失败。
- 固定 CoverageSummary 的计数口径、RetrievalRun 的终态状态规则，以及 A 向 C 提供浏览器安全运行投影的最小字段。
- 明确本步骤不开始逐来源统计、自动重试、多轮检索、持久恢复或进度流。
- 新增 B 的多来源搜索批次固定样例和 C 的浏览器 Remote 返回固定样例，并分别列出行为、修改范围和验收条件。

## 检查结果

- 学术相关聚焦测试：32 个测试文件、351 个测试通过。
- 合并后的控制器同时保留已批准 Brief 重建和多来源检索，并正确排除未注册的全文来源。
- 工作区在本次文档修改前无未提交文件。

## 下一步

1. B 按交接规则让多来源搜索返回部分成功和来源失败，不清空其他 Provider 的成功结果。
2. A 消费 B 的搜索批次，在工作流和 Remote 返回中生成 RetrievalRun 与 CoverageSummary。
3. C 使用固定运行投影夹具并行开发主 Web 的运行状态、失败和报告展示。
