# Academic 洞察合成接口交接

## 范围

成员 A 的第一步：为 Evidence → Insight → Report 固定输入、模型输出、JSON 场景和后续所有权。保留现有全文获取与解析能力；本次不实现模型合成、报告渲染或新的 Remote 接口。

## 完成内容

- 新增[交接说明](../模块分工/academic-synthesis-handoff.md)，复用已批准 Brief、证据卡、证据定位、Claim 和快照。
- 固定按 Brief 版本绑定的问题索引、逐问题覆盖、带引用陈述、章节组织和宿主生成身份规则。
- 固定证据不足时报告为空、部分回答明确披露、结构检查不能替代语义审核的目标行为。
- 提供输入、输出和 12 个验收场景的合成 JSON；全部标明 synthetic，原文与哈希均不代表真实论文。
- 新增 proposed Agent Note；检查既有报告基线，保留其可追溯性和审核规则，没有归档现有决策。

## 验证与限制

字段来自当前工作区共享类型。三个 JSON 文件解析、两篇样例论文的版本与哈希关系、证据引用、两个问题的索引、章节引用以及 12 个场景补丁路径均检查通过。文档快速检查 15/15 通过。JSON 样例不是运行时校验器，模型标记 answered 不能证明内容正确；真实课题的正文篇幅与内容尚需后续实现和验收。

完整 `doc-sync` 首轮 32/33 通过，唯一失败为此前模型调用记录变更导致持久化目录中的源码行号过期。已重新生成英文目录、同步中文行号并更新配对记录，`verify-persistence-catalog` 单项复查通过；本次未改动持久化事件类型。

## 后续顺序

A/B 优先稳定真实全文到有效证据的路径，再由 A 接入真实合成；用户明确指出模型抽取不稳定会阻塞后续全部工作。C 可用固定证据并行开发合成与报告，但只能证明模块行为。A、C 按交接落实类型和 Plan 支持性检查，模型调用及日志归 A。保留已有全文解析；最终由三方使用同一批准课题和真实证据验收。
