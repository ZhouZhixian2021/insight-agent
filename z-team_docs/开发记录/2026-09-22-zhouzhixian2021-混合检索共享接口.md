# 开发记录：混合检索共享接口

## 基本信息

| 项目 | 内容 |
|---|---|
| 日期 | 2026-09-22 |
| 负责人 | A，ZhouZhixian2021 |
| 协作人员 | B、C 按固定接口继续各自模块 |
| 个人分支 | `dev/zhouzhixian2021` |
| 任务分支 | 无 |
| 提交 | 待提交 |
| 远程状态 | 本地已实现，尚未推送 |

## 目标

完成混合检索计划 A-H1：在不启动真实 Web 混合检索的前提下，先固定 A、B、C 并行开发需要的计划策略、论文引用识别与核验、浏览器安全运行投影和无网络固定样例。

## 实际完成

- 在 `@deepseek-ai/dsh-academic-source` 固定 Web 候选、DOI/arXiv/ACL/PMLR/CVF 引用、纯识别函数、Provider 可选精确核验方法和逐引用成功/失败结果。
- 在 Academic Controller 固定可选的计划级检索渠道、直接检索 Provider、核验 Provider 与两类数量上限。
- 固定 C 使用的混合检索阶段、Web 候选状态、引用状态及浏览器安全统计；Web URL、引用、核验尝试和去重论文不混算。
- 增加全合成的五类引用夹具和契约测试，包含没有 DOI/arXiv ID 的官方记录、非论文丢弃、核验成功、核验失败与重复合并。
- 增加双语包说明和架构决策记录，明确 Web 摘要不是证据，当前运行时仍保持既有单通道行为。
- 通过仓库生成器更新 Cordis 公共类型目录与配置目录来源行号，并同步中文配置目录和双语配对记录。

## 实际检查

- Academic Source 与 Controller 相关测试：4 个文件、97 个测试通过。
- 主机端与浏览器端 TypeScript 工程构建检查通过。
- `pnpm run lint:contracts-ready`、`pnpm run constraints` 与 `pnpm run verify-export-jsdoc` 通过。
- `pnpm run test:docs`：15/15 通过；`pnpm run doc-sync`：33/33 通过。
- 四组本次双语文档及配置目录配对检查通过；Agent Note 格式检查通过。
- `git diff --check` 通过。

## 风险与限制

- 当前只完成接口基线；计划解析器尚未产生或校验混合策略，工作流尚未调用 `ctx.web.search()`，Remote 也尚未填充 `hybridRetrieval`。
- Provider 只获得可选核验方法契约；五类引用识别、权威记录定位、网络错误分类与真实来源测试属于 B 的后续实现。
- C 可立即使用固定夹具开发页面，但在 A-H3/A-H4 合并前不能把固定数据误认为真实运行结果。

## 下一步

- A 合并 A-H1 后继续 A-H2：扩展计划生成、审核和严格校验。
- B 从最新 `master` 开始 B-H1/B-H2：引用识别与按标识符权威核验。
- C 从最新 `master` 使用固定夹具开始 C-H1/C-H2：计划审核、阶段与统计展示。
