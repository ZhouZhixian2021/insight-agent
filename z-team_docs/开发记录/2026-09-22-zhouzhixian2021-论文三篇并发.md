# 论文三篇并发

## 范围与归属

A、B、C 恢复按模块分工。本次由 A 负责工作流调度与 Controller 配置，复用 B 的来源、全文与证据接口以及 C 的结果展示；不修改模型、输出预算或证据要求。

## 实现

- [Controller](../../packages/api/academic-research-controller/src/index.ts)新增 `paperConcurrency`，默认 3，传入工作流；设为 1 可恢复串行。
- [流水线](../../packages/academic/workflow/src/pipeline.ts)以有界队列并发获取、解析和抽取论文，按候选顺序汇总。直接调用库省略并发数保持 1，历史调用方不被隐式改变。
- 在途任务预留纳入名额，成功论文与预留数不超过 Plan 上限；排除或失败后补选。证据达标后不启动新论文，已启动任务正常收尾，再开始洞察。
- 取消或超时停止新任务并等待在途调用收尾；保存日志失败中止其他任务并保留致命错误。成功证据、统计和失败仍按原接口返回。

## 边界

前序慢任务可能延迟顺序结算与补选；尚未改为按完成顺序选文。并发 3 不代表三倍速度，可能遇到提供方限流。未增加自适应检索、跨运行恢复或实时进度。未重启当前 Web、未发起付费模型研究，真实耗时收益仍待重启后的同题对照验收。提交目标为个人分支 dev/zhouzhixian2021，由成员 A 后续创建 PR。

## 验证

- 流水线、模型持久化、并发调度和 Controller 聚焦测试：147/147 通过；最后一次队列代码调整后，并发专项 11/11 再次通过。
- 三个模型调用并行时，请求与结果均保留独立、匹配的持久化记录。
- 最小 SDK 配置的真实组合回放：原串行补选与新增三篇并发场景 2/2 通过；并发场景观察到峰值 3，失败后补选并生成报告。
- Host/Client 类型检查、全仓 lint（含 Host 构建）通过。
- 文档测试 15/15、完整 doc-sync 33/33 通过。
- 以上为自动化验证；尚未测量真实模型服务上的加速比例。
