# 模型回答检查器

本次在成员 A 的 [workflow](../../packages/academic/workflow/README.zh.md) 中增加 parseEvidenceDrafts，按既定输入输出计划检查完整模型回答，不修改 B、C 的业务实现。

检查包括 JSON 数组、六类证据卡字段、枚举、缺失值状态和可选备注。拒绝额外字段、编造身份和模型无权生成的 failed 状态；整份回答任一条格式错误则拒绝整份，空证据数组仍是合法结果。错误仅包含字段位置及原因，不回显原始回答。段落范围和原文对应关系仍由 B 检查，语义审核仍是后续独立步骤。

验证：工作流 3 个测试文件、83 项测试通过，聚焦覆盖率语句/分支/函数/行均为 100%；源码与测试类型检查及聚焦 oxlint 通过。workflow 聚焦构建及普通 Node 导入解析器检查通过；中英文配对记录已更新，test:docs 为 15/15，git diff --check 通过。测试使用合成回答，通过真实 B 抽取函数验证交接，不代表真实模型或网络验收。未重复全仓构建和完整 doc-sync，本次未处理此前已知的 C 包构建问题。

后续接入 DSH 模型调用、输出结束状态、完整输入预算和 Session 请求/结果记录。超长论文按用户决定暂停该论文、继续其他论文，分批抽取留待后续。本次未提交或推送。
